# Origem da API. Em desenvolvimento o proxy do Vite ja cuida de /api, entao so
# defina isto se a API estiver em outro host.
VITE_API_URL=
# Alvo do proxy do vite dev server (padrao: http://localhost:8000).
VITE_API_ALVO=http://localhost:8000
